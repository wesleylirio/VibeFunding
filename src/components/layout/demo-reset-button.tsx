/**
 * Reset is intentionally not rendered in the product UI.
 * Use `npm run seed:reset` (documented in README).
 * The API `POST /api/demo/reset` remains available for scripts.
 */
export function DemoResetButton() {
  return null;
}
